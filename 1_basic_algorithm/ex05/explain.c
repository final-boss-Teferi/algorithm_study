// 문제 : 중앙값을 구하는 함수는 다음과 같이 작성할 수도 있습니다.
//       그러나 실습 1C-1의 med3함수에 비해 효율이 떨어지는데, 그 이유를 설명하세요.
//
//int med3 (int a, int b, int c)
//{
//	if ((b >= a && c <= a) || (b <= a && c >= a))
//		return a;
//	else if ((a > b && c < b) || (a < b && c > b))
//		return b;
//	return c;
//}

// 답 : 실습 1C-1의 경우 최악의 경우 조건문을 3회 확인하면 되지만,
//	    이 예제의 함수의 경우 최악의 경우 조건문을 8회 확인하게 됩니다.
//      그리고 최선의 경우인 2회 확인하는 때도 1번 밖에 존재하지 않는 등
//      평균적으로 연산 횟수가 늘어나 효율이 떨어집니다.